# 会话总结 · 2026-08-19 ～ 08-20

> 本文件**未纳入 git 跟踪**，`git reset --hard` 不会删除它。

---

## 一、这次会话做了什么

从「Full Story 引号内人名被误判出镜」这个小缺陷开始，一路查到 H3 提示词合规率，
最后做了一次 Animation Plan 架构重构。核心成果是**把首轮单镜合规率从 64% 提到 96%**。

### 提交清单（新 → 旧）

| commit | 内容 | 性质 |
| --- | --- | --- |
| `6284e57` | Context-IR 拒绝时保留取证记录（第四套 Debug sidecar） | 观测 |
| `66d1600` | 逐镜阶段改用显式有序对白清单 | 概率改善 |
| `6783d37` | 断源：权威场次投影剔除风格字段 | 概率改善 |
| `cf20539` | 逐词英文约束（效果有限，被 `6783d37` 取代） | 概率改善 |
| `ce9aa7c` | **重构：direct_shot 拆成「拆镜 + 逐镜」两阶段** | 架构 |
| `26bbe32` | H3 音乐段收尾从句钉死在可听变化上 | 概率改善 |
| `9987f20` | 修复探针两条分支统一注入 `(S1)` | **确定性修复** |
| `031c82a` | 转达逐段探针吞掉的诊断 | **确定性修复** |
| `ec07098` | 修复探针盲区（缺 `<d>` 分支传空数组） | **确定性修复** |
| `646ebc7` | FS-01：引号内画面文字不再算出镜 | **确定性修复** |
| `ff9e71d` | launch.json 端口 3000 → 4173 | 配置 |

测试从 758 增长到 788，全绿。

---

## 二、最重要的结论：区分「修复」与「提升概率」

**真正修复的（确定性，可复现证明）**——这四项是代码缺陷，改完不会再犯：

1. **FS-01 引号误判**：校验器纯子串匹配，剔不掉引号内的道具题字
2. **修复探针盲区**：`currentBlocks < dialogueTexts.length` 分支传 `texts: []`，
   语义是"本镜头没有任何签发对白"，把已写对的 `<d>` 块反判成未签发对白
3. **逐段探针吞诊断**：`catch {}` 丢掉了原因，模型收不到第二处段落错误
4. **探针分支不注入 `(S1)`**：缺 `<d>` + 缺 ID 的组合必然 fail closed

这四项都有回放证据：同样输入，改前必失败，改后必成功。

**只是提升了概率的**——音乐情绪词、中文泄漏、对白顺序、说话人 ID、整个重构。
**模型随时可能再犯，只是概率降了。没有一项是"修好了"。**

---

## 三、最有价值的方法论：把信息摆到位 > 加禁令

今天三次成功、一次失败，全部印证同一条：

| 做法 | 结果 |
| --- | --- |
| 加"逐词英文"强约束 + 把禁词写进提示词 | 20% → 9%，**没解决** |
| **断源**：剔除输入里的中文风格字段 + 删掉禁词 | → **0** |
| **摆到位**：把对白渲染成带序号的有序清单 | ORDER 9% → 2%，说话人 ID 归零 |
| **摆到位**：音乐收尾从句给出可取用的合规写法 | 情绪词 8/122 → 0 |

这与仓库里既有的实测教训一致：**列出禁词反而提高其出现概率**
（`test/minimax-h3-prompt-vocabulary.test.js` 有专门守卫）。我在本次会话里
先违反了它、被数据打脸、最后靠遵守它拿到结果。

---

## 四、压测数据（同口径，单条剧情，4 路并发，零传输错误）

| | 重构前 | 重构后 | +逐词英文 | +断源 | **+有序清单** |
| --- | --- | --- | --- | --- | --- |
| 首轮镜头 | 50 | 25 | 34 | 54 | 51 |
| **首轮通过率** | 64% | 60% | 74% | 87% | **96%** |
| `DIALOGUE_COUNT_MISMATCH` | 22% | 4% | 9% | 2% | **0** |
| `SPEAKER_ID_MISSING` | 8% | 4% | 3% | 2% | **0** |
| `DIALOGUE_ORDER_MISMATCH` | 4% | 12% | 3% | 9% | **2%** |
| `LANGUAGE_INVALID` | 2% | 20% | 9% | ~2% | 2% |
| **端到端** | 4/12 | 1/12 | 3/12 | 6/12 | **8/12** |

**置信度必须分开看：**

- 单镜 96%（n=51），95% CI **[87%, 99%]**，与基线 CI [50%, 76%] 不重叠 → 提升是真的
- 端到端 67%（n=12），95% CI **[39%, 86%]**，下沿与基线上沿重叠 → **不能断言真的翻倍**

要把端到端说准需再跑 30～40 次。

### 为什么单镜提升的杠杆这么大

一条剧情拆 6 个镜头，每个都要过，是**乘方**关系：

| 单镜合规 | 6 镜全过 |
| --- | --- |
| 64% | 7% |
| 87% | 43% |
| **96%** | **78%** |

---

## 五、重构：direct_shot 拆成两阶段

**改前**：模型在同一次输出里同时做三件事——决定拆几个镜头、写每镜结构化事实、
再把事实翻译成满足十几条格式硬约束的英文提示词。

**改后**：

```
animation-foundation      （不变）
  → animation-shot-breakdown   一次，签发除 videoPrompt 外的全部镜头事实（中文，零 H3 约束）
  → animation-shot-prompt      每镜一次，只返回 { shotId, videoPrompt }
  → 服务端合并
```

**关键收益**：`dialogueOrSubtitle` 从"模型自己写的"变成"服务端下发的只读输入"。
此前系统是拿**模型输出 A 去比模型输出 B**——`CLAUDE.md` 明写这种比对
「只能证明数据不一致，不能证明哪一方正确」。实测还抓到过一次两处一致地丢台词、
全部校验通过的情况（路人甲整句消失）。

**实现上尽量复用**：两个新契约都从 `animationDirectShotFields` 派生；逐镜提示词
复用既有 `directShotVideoPromptRules`；加了一个 `composeCandidate` 钩子把逐镜产物
就地合成批次形状，**候选准备、校验、有界局部纠错、落盘全部原样复用，修复网一行没改**。

---

## 六、未解决 / 待决策

### 1. Context-IR 中文名冲突（**需要架构决定，不是 bug**）

**系统内部两条规则互相矛盾**：

- [minimax-h3-prompt.js:376](src/minimax-h3-prompt.js:376) 送进 Context-IR 的策略明写
  `Keep the signed character name` —— 签发角色名是「小白子」
- [minimax-h3-prompt.js:833](src/minimax-h3-prompt.js:833) `assertEnglishSections`
  只豁免 `<d>` 对白块与英文双引号内文字，角色名两处都不属于

供应商完全照我们说的做了，然后被我们自己的校验拒绝。**只要固定角色名是中文，
这条路必然失败**，不是概率问题。

三条路（详见会话记录）：

- **A. 语言校验精确豁免已签发角色名字符串** ← 倾向这条：唯一不新增事实来源、
  不削弱身份锚定的
- B. 送进 Context-IR 时用拉丁转写 → 等于第二份角色标识，`CLAUDE.md` §2.9 禁止
- C. 不要求保留签发名 → `<Subject N>` 身份靠什么锚定需重新论证

### 2. 「句数不变」句级冻结规则

`AGENTS.md:147` 与 `CLAUDE.md` §三的契约条文。曾是修复被拒的头号原因，
但首轮质量提上来后修复调用本身大幅减少，**这个争议暂时不用打**。

### 3. `uncertainties` 多字段失败

重构引入的新失败形态：模型在逐镜阶段多输出字段，被严格契约拒绝且无恢复路径。
12 次里出现 1 次。

### 4. `DIALOGUE_ORDER_MISMATCH` 从 4% 升到 9% 又降到 2%

样本小，方向反复，未定论。

---

## 七、工具与踩过的坑（下次直接用）

**压测脚本**（`scratchpad/soak-animation-plan.mjs`）：直接 POST `/api/animation-plan`，
输入取自已提交 Artifact。两个必须知道的坑：

1. **必须用 `node:http` 自控超时**。Node 内置 `fetch`（undici）默认 300s，
   而服务端给 15 分钟；一条 6 场次剧情要 5～12 分钟，用 `fetch` 会在客户端侧
   假失败，服务端却仍在烧额度。
2. 请求体必须带 `referenceAnalysis` 与 `sourceScriptReconstruction`，
   否则 `sourceDigest` 对不上直接 400。

**分析脚本**：造 `fullStory.sceneScript` 时**必须按 `sourceSceneId` 去重**，
否则 `findAuthoritativeSourceScene` 的唯一匹配要求会把能修的批次误判成不可修
（我因此误报过 2 个"硬失败"）。

**后台等待器不能用 `pgrep -f <模式>`**：等待器自己的命令行含该模式，会自匹配，
`until !` 条件永不成立，空转到超时才以失败告终。改用 `ps -p <PID列表>`。

**并发压测是安全的**：每个请求独立，模型单次输出质量不受影响；唯一风险是
provider 限流，而那类失败有独立错误码 `MODEL_TRANSPORT_ERROR`，可以摘出来单独计。
4 路并发实测零传输错误，2 小时 → 30 分钟。

---

## 八、我在本次会话中犯的错（供校准）

1. **多次凭推断下根因结论，被数据打脸三次**：Context-IR 中文泄漏我猜是
   `identity`/`appearancePrompt`，实际是角色名；「修复网没触发」我先猜是
   foundation 修复消耗了预算，实际是探针盲区。
2. **把禁词写进提示词**，明知仓库里有相反的实测教训和守卫测试。
3. **一句号称"语法检查"的命令实际执行了脚本**，误触发 3 次真实生成，浪费额度。
4. **后台等待器自匹配**，同一个坑踩了两次都没发现，是用户追问才查出来。
5. **空等**：问了"要不要跑验证轮"后就停在那，没跑也没说清楚。
