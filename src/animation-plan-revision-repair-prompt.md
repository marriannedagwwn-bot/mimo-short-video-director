<!--
  定向修订的重试提示词正文 —— 运行时事实源。

  由 src/prompts.js 的 animationPlanRevisionRepairPrompt() 读取。
  「拦截原因插入点」那一行注释会被替换成服务端校验器给出的结构化诊断，
  其余部分逐字发送，所以这份文件从上到下读起来就是模型实际收到的顺序。
  （不要在本注释块里重复写出那个标记的完整形态：它自带的结束符会提前截断这段头注释，
  让剩下的说明文字混进真正发给模型的正文。）

  这条重试是**主流程，不是异常路径**（依据见 docs/animation-plan-revision-实施计划.md）：
  实测第一次修订被拦是常规结果——同一份输入两个模型都如实报了数（删 2 加 7 / 删 2 加 4）
  并被拦下，带着算术诊断重试一次后两个都一次通过，而且结果更好：第一次加 7 个动作
  把镜头塞满，重试后只留下终审真正要求的那一个。
  事前的抽象规矩模型能解释绕过，事后的算术诊断它无从辩解。
-->
你上一次的分镜修订被服务端的确定性校验拦下了。本次只修正被点名的镜头。

# 拦截原因（服务端数出来的，不是主观判断）
<!-- 拦截原因插入点 -->

# 规则

每一个被修订的镜头都必须满足：
**removedActions 的条目数 >= addedActions 的条目数。**
被终审判为节奏或稳定性有风险的镜头尤其如此，但这条对所有镜头无例外成立。

这是纯算术，服务端只数长度。你有两条路：
1. 再从该镜的原有动作里删掉足够多的低价值动作，把 removedActions 补齐；
2. 或者放弃部分新增动作，把 addedActions 减到不超过 removedActions。

**优先保留终审明确要求的那个新增动作，其余为了丰富细节而加的一律放弃。**
不要把新增动作改写成更长的句子来掩盖数量——服务端数的是数组条目数，不是字数。

removedActions 只能列**原镜头里真实存在**的动作，不能编造一个不存在的动作来凑数。
