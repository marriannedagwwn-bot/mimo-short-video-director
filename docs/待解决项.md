# 待解决项

**这份文件只登记「已确认存在、但尚未决定怎么改」的问题。** 不是 TODO 清单，也不是路线图：
每一条都必须写清**证据**、**为什么还没改**、以及**已经排除掉的做法**——后者最重要，
否则下一个人会重走一遍今天已经走死的路。

已经有确定结论的东西写进 `AGENTS.md` / `CLAUDE.md`，不写在这里。
条目解决后从本文件删除，并把结论写进对应契约章节。

---

## 1. 镜头动作过载，源头在 Full Story 而不是分镜

**状态**：已定位根因，未决定怎么改。

**现象**：定向修订对某些镜头永远通不过净预算闸门（`删 ≥ 加`），无论重试多少次。

**证据**（2026-09-07 真实回放，《天台上的纸飞机》）：

- 终审自己判 A03「12 秒内要完成折、画、飞、回信、接、再折再飞、猫追影、按发笑等多个事件，
  动作链过长」+ `ai_risk`，**同时**又给这一镜派了 6 条 issue 和 2 条 upgradePath，合计要加 7 样东西
- 修订模型**如实照做并如实报数**：第一次 A03 删3加7、A04 删1加2、A06 删3加4
- 带算术诊断重试一次后 **A04 与 A06 都修好了**（删2加2 / 删4加4），只有 A03 从加 7 降到加 6 仍超支
- 两次 provider 调用、19 分钟、58018 token，最终 fail closed

**根因不在修订，在上游**：A03 的 12 秒来自 `fullStory.sceneScript` 的 S3（`00:18-00:30`），
而 S3 的 `visibleAction` 是全片最长的一条（163 字），塞了约 14 个动作：

> 抽白纸 → 折纸飞机 → 画哭脸 → 侧身飞出 → 划过夕阳 → 对面飞回 → 接住打开 →
> 嘴角上扬 → 又折一架 → 画太阳 → 再飞过去 → 猫追影子蹦跳 → 风吹乱头发 → 按住头发眯眼笑

**修订在架构上就修不了它。** 镜头骨架由 `deriveDirectShotSkeleton()`（`src/direct-shot-timeline.js`）
从 Full Story 场次**确定性派生**，修订只能改 7 个可写字段，**动不了 `durationSeconds`、也不能拆场**。
它只能在固定的 12 秒里搬动作，而 12 秒本来就装不下这些。

**方向（未验证）**：在 Full Story 阶段就约束单场动作密度——要么写少一点，要么把 S3 拆成两场。
`§2.10` 已有「生活质感」「萌点必须是大动作」等生成约束，但**没有任何一条约束单场动作数量**。

**已经排除的做法（都试过或已论证不可行）**：

- ~~在终审侧加硬闸门，不许它给过载镜开净增方子~~ —— **2026-09-07 实测有害，已回退**。
  终审没有重试，硬闸门架在它自己的输出上就等于整个阶段出不了报告：三次真实调用全失败，
  而此前它是能出报告的（09:50 那次 493 秒、7.03 分）。
- ~~把「补写既有事实」从净预算里豁免~~ —— 实测推翻。A03 那两条 `prop_state_break` 的修法是
  「把纸飞机放在书包上」，**是货真价实的物理动作**，豁免它等于放宽闸门（§5 第 1 条）。
  唯一构造上非动作的 `dialogue_not_in_prompt` 只值 1 个额度，救不了 A03（额度 4 < 加 6）。
- ~~让模型自报 `actionComplexity`~~ —— `CLAUDE.md:343` / `AGENTS.md` / 实施计划三处明文禁止：
  把判定权还给模型，正是净预算当初要拿走的东西。
- ~~拿维度分数当门槛~~ —— 同上三处禁止，单维分辨力最差（两个模型评同一份能差 ±1.0）。

**不得因为某个输入通不过就放宽净预算闸门。** 它在真实数据上是有效的：三次真实调用全都
抓到了终审开的净增方子。

**新增一个上游触点（2026-09-10）**：命题定向修订会改 `storyOutline[].action`，而那正是这条链的
最上游——命题的动作密度决定 Full Story 的场次密度，再决定镜头的挤不挤。
**那一档刻意没有设动作数量台账**：净预算是在分镜那边三次提示词加码失败之后才引入的，命题阶段
没有对应实测，凭推断加闸门违反「不得顺手扩大范围」。现在的做法是提示词写明「不要靠加戏解决
问题」，并在预览里并排展示原文与修订稿、数出动作链字数（首次真实回放：389 → 394 字，没有变长）。
**要不要给命题修订也加台账，等回放数据够了再定**，不得凭这一条推断先加上。

---

## 2. `ModelPipelineError` 吞掉供应商原文

**状态**：**2026-09-10 已修**。

原来 `src/server-error.js` 的两个分支口径不一致：`ModelResponseError` 分支带
`describeProviderError({payload: error.raw})`，`ModelPipelineError` 分支只回
message / diagnostics / attempts。coordinator 抛的正是后者，所以**凡是走 coordinator 的阶段，
供应商自己的错误消息一律被吞掉**：定向修订、Full Story、Animation Plan 一直如此。

实际代价：2026-09-07 一次重试被 HTTP 400 拒绝，350 字节的错误原文存在内存 `AttemptStore` 里
（`rawOutputRef` 指着它），但响应里 `details: []`、`providerError: null`，
**根本无法判断 400 的原因**，调查就此中断。

修法就是那条记着的猜测：coordinator 抛出时传了 `cause`，`ModelPipelineError` 用
`super(message, {cause})` 保留它，所以 `pipelineProviderError()` 在 cause 是 `ModelResponseError`
时调现成的 `describeProviderError`，结果放进新增的 `providerError` 字段。
**纯增字段**，既有七个逐字不变（§5.1「只增不减」）；没有供应商响应体时返回 `null`，调用方回退
原文（「编一句『可能是网络问题』比不解释更糟」）。**不重复记 attempt**——原文本来就在
`error.attempts[].rawOutput` 里。浏览器侧不用改：`providerErrorText` 早就在渲染它。

**那个 400 本身仍未查清**，只是下次再遇到能看见原因了。已排除的一条假说：不是 token 上限
——`retryTokenLimit(16384) = 20480`，而实测 qwen3.7-max 对 16384 / 20480 / 32768 三个值
全部 HTTP 200。那次失败发生在终审阶段（用 `storyModel`，可能是 3.8），模型不同。

---

## 3. 定向修订失败时只报第二次诊断

**状态**：契约与实现不符，未改。

`CLAUDE.md` §2.14 写的是「第二次仍被拦即 fail closed（保留原 Plan、**把两次诊断如实报出**）」。
2026-09-07 实测响应里 `details` 只有第二次那一条（A03），第一次那三条（A03/A04/A06）不在响应里；
`attempts[]` 有两条尝试的元数据，但不带各自的 `diagnostics`。

现在能从 debug 侧车的原文里拿到，但**那是 debug 侧车，不是响应契约**。

**2026-09-10 起有了现成写法可以照搬**：候选对照评审接重试时一开始就做对了这一条——
`candidateReviewPipelineFailure()`（`src/workflow.js`）在两次都被拦时重建 `ModelPipelineError`，
把每一次的诊断合并进 `diagnostics` 并各带一个 `attempt` 序号（额外字段会进
`ValidationDiagnostic.metadata` 并被 `toJSON` 带出去），其余字段逐字照抄、不改归属与状态码。
修订那边套用同一个函数即可，**但那是独立改动，不要顺手做**。

---

## 4. 分镜终审没有重试（候选对照评审已在 2026-09-10 补上）

**状态**：终审仍是缺口；**评审那一半已修**。

终审走 `generateStageJson` → `generateValidatedJson`，**只发一次 provider 调用**，
对已解析候选 fail closed。内容错误（三张覆盖表数量对不上、引用了不存在的镜头号）
没有任何矫正机会，而这类错误恰恰是模型最常犯的。

**候选对照评审此前走的是同一条路，2026-09-10 实测撞上了。** 同一个端点、同一个模型
（qwen3.7-max）、两份来自不同参考片的合法候选：《打枣》包一次通过，`recommendedOrder`
四个 id 写全；《捐旧衣服》包**只写了 V1**，被既有闸门 `CANDIDATE_REVIEW_ORDER_NOT_PERMUTATION`
判失败——HTTP 502、¥0.27、90 秒，用户手里什么都没有。那份输出的其余部分（机制清单、
逐候选核对、因果自洽）**是否可用无从得知，因为整份被丢弃了**。

这条闸门是 2026-09-06 就有的，与当天新增的机制清单契约无关；**它证明的是形状问题**：
一份两千字的结构化报告里漏抄三个 id，模型自己不知道，而这一档没有任何路径告诉它。
`docs/animation-plan-review-落地方案.md` 早就写过同一句结论——「事前在提示词里定规矩→
没用；事后拿数字打回去重做→有用」，且「**修订必须设计成「允许第一次做错」**」。

**评审已按这条结论落地**（`CLAUDE.md` §2.12b ⑦）：改走 `modelCallCoordinator.runJson`，
`maxProviderCalls: 2`，禁止第三次；重试只发校验器数出来的诊断、不发回失败的报告；
`metadata.storyCandidateReview` 如实上报调用次数与每一次被拦的诊断。
**校验逐字未动**——改的是「错了之后怎么办」，不是「什么算错」。

**终审还没做，两条原因**：①2026-09-07 那次尝试**重试确实触发了**，却被第 2 条那个 400 拦住，
整块已回退；那个 400 现在能看见原文了（第 2 条已修），但根因仍未查清。
②终审的失败形状没有像评审这样的实测数据，不得凭推断顺手扩大范围。

**注意**：不能直接在 `generateValidatedJson` 上加重试——它是**八个阶段共用**的单次调用路径
（评审搬走之后是八个）。唯一的出路就是像评审这样，让需要重试的阶段自己走 coordinator。

**这个形状不止终审一个，创意简报当天也撞上了（2026-09-10）。** 从源视频跑整条链时，
`/api/brief` 502：`allowedNarrativeComponents[0]`（送达任务）的 `howToReuseSafely` 写了
「携带物品前往某处交付」，`citationCoverage` 只有 0.30 被拦下——HTTP 502、¥0.31、102 秒，
而它前面的 analyze 与 reconstruct 已经花掉 ¥0.24 且**整条链就此中断**。

这条诊断和评审那条一样具体、一样可执行（「覆盖率 0.30」是个数字），**完全够打回去重做**。
所以真正的结论比这一条原来写的更宽：**凡是有确定性内容闸门又走 `generateValidatedJson` 的阶段，
都是同一个形状**，链越长、越靠前的那一环失败越贵。要不要把 brief 也搬到 coordinator 上，
是一个独立决定，**不得凭这一条推断先做**。

---

## 5. 传输失败率约三分之一，没有换模型兜底

**状态**：已记录在 `CLAUDE.md` §2.14，长期未决。

两种形态：`fetch failed`（3–5 秒，没烧 token）与 `terminated`（几百秒才断，token 已烧）。
`docs/animation-plan-review-落地方案.md` 已决定「评审允许换模型但必须在报告里写明」，
**但终审与修订两个阶段都还没有实现换模型兜底**。

2026-09-07 已修掉其中一个具体缺陷（推理期断线被误判为不可重试，见 `AGENTS.md` §2.7），
但换模型兜底本身仍未做。

**已排除**：~~把一次评审拆成几次小请求~~ —— 实测数据不支持。推理占 completion token 的 82%
（拆输出不减它）；prompt 要 ×N（17k/次）；而按镜头拆会让 `propTracking` 与 `continuity`
看不到跨块的东西——真实报告里 ISS-06「书包在 A01 和 A03 被使用，A02、A04 没有交代」恰好跨块，
而那正是这个阶段存在的理由。三张强制覆盖表也要求一次看全片。

---

## 6. 报告里 21% 的输出是没有分辨力的分数

**状态**：已量化，未决定是否精简。

`dimensions`（12 个维度各带 id/score/reason）占终审报告输出的 **21%**（3449 / 16256 字符），
而 `CLAUDE.md` §2.14 自己写着这个分数**没有分辨力**：实测两个模型评同一份 Plan
总分只差 0.06，而单个维度能差 ±1.0，「有用的是 `dominantDefect`、`issues` 与 `upgradePath` 的具体内容」。

砍掉或精简它是零信息损失的减输出，但那是一处契约变更，需要单独决定。

---

## 7. 几个阶段还没做过真实回放

**状态**：评审与剧情体检都已跑通（体检 2026-09-12 首次真实调用）。

- `storyCandidateReview`（候选对照评审）—— 2026-09-09 首次真实回放；**但当天新增的
  `sourceMechanisms` 契约又是零真实调用地合并的**，直到 2026-09-10 才补跑
  （见下面「补记」）。同一个坑在同一个阶段上踩了两次。
- `storyCandidateRevision`（命题定向修订）—— 2026-09-10 建成当天就真实回放过一次：
  《迷路的星星灯笼》V3，103 秒一次通过，评审报出的那条 `purpose_nullified` 被**结构性**解掉
  （任务从「归还失物」改成「帮助完成心愿」，第 1 拍埋线索、第 3/4/5 拍跟着咬合），
  动作链 389→394 字、时长 65→65 秒、其余命题逐字节不变。**一次回放不构成结论**
- `storyQualityReview`（剧情体检）—— **2026-09-12 首次真实调用**，见下面「补记二」

「测试通过」不等于「真实调用会留下记录」：终审那次恰恰证明了这一点——正是真实调用
才发现 `continuity` 枚举缺口，而那是任何单元测试都测不出来的。

**补记（2026-09-10）**：`sourceMechanisms` + `coherenceChecks` 契约合并当天，侧车里
`storyCandidateReview` 共 6 次真实调用、**全部是旧契约、新契约 0 次**。补跑两个包的结果
写在下面，同时暴露了第 4 条那个没有重试的形状：

| 包 | 结果 |
| --- | --- |
| 打枣 | 一次通过，130 秒 ¥0.27。清单 4 条（上限）逐条锚到原片场次 S2–S6；12 处引用全部命中，新加的两条闸门一条都没触发；因果自洽命中 2/4 个候选 |
| 捐旧衣服 | 第一次 502（`recommendedOrder` 只写了 1 个 id，详见第 4 条），第二次 `fetch failed`（3 秒没烧 token，第 5 条那种），**第三次通过**：117 秒 ¥0.27，同样 4 条清单、9 处引用全部命中、因果自洽 2/4 |

**这批数据能支持的结论很窄**：它证明「模型能一次写出合规的共享机制清单并按 id 引用」，
且旧契约那种「4 个候选写出 8–9 条各不相同的原片机制」两个包都没再出现；
**不证明**清单读对了原片，也不足以谈命中率。两条要如实记着的限制：

1. 两个包都有**没被任何候选引用**的机制（打枣的 M4 零引用）——清单写满 4 条不等于 4 条都用得上。
2. 判 `pass` 的三个候选都没报出自洽问题，所以
   `STORY_CANDIDATE_REVIEW_PASS_WITH_COHERENCE_BREAK` **至今仍未在 live 触发过**。

三次调用里只有一次是「一次就成」，另外两次一个撞闸门、一个撞传输——**这一档的实际可用率
是 2/3 且第一次成功率 1/2**，成本全部由用户承担。这组数字促成了第 4 条那个带诊断的重试。

**接上重试之后又跑了两次（同一个包，121 秒 / 112 秒，各 ¥0.27）：两次都一次就成，
所以重试路径在 live 至今没有真实触发过**，与 `pass` 闸门的处境一样——只有单元测试证明有效。
真实回放这两次证明的是「改动没有破坏正常路径」：报告形状正常，`metadata` 如实上报
provider / model / 调用次数。**不得据此宣称重试能救回来。**

顺带一条与本次改动无关但值得记的观察：**同一份候选、同一个模型，三次回放的结论明显不同**
——`verdict` 从「2 pass / 2 revise」到「2 revise / 2 drop」，因果断裂从 2 处到 3 处，
`recommendedOrder` 三次都不一样。这与前面记的「判定质量在包与包之间很不齐」是同一件事，
只是这次看到的是**同一个包内部的不稳**，更强化那条结论：
`verdict` 与 `recommendedOrder` 不得当成自动选择依据。

---

**补记二（2026-09-12）：`storyQualityReview` 首次真实调用，并暴露一条此前没写进契约的结构性局限。**

输入是候选 V2《罐装阳光与太阳味》展开出的一份 6 场剧情（45.4 秒，提示词 10,139 字符 /
4,975 token，输出 4,937 token 其中推理 3,571 = 72%，`finish=stop`）。覆盖率核验一次通过：
6 场 + 2 条留存计划数量相等、id 逐位对齐、回显包含原文。

**它抓到一处纯人工读漏的东西，而且正是那一份最被称赞的段落。** S4 判 MAJOR：
主角整个人扑倒、用外套把光斑捂在被子上，再从衣服底下用罐子扣住——
「**这一核心高潮动作会被外套完全遮挡，观众无法直观看到扣住并盖上盖子的童趣瞬间**」，
且缝隙容易漏光。人工阅读把它当成一个**故事节拍**，体检读的是一个**镜头**。
另一条 MINOR 同样成立：S1「正双手递木夹子，立刻转身双手交叠鞠躬」缺过渡动作，
双手同时占用在物理上无法实现。**这两条正是这个阶段设计初衷的命中。**

**但它把六场全判 `depicted`、两条留存计划也全判 `depicted`。** 同一份剧情里人工
读出的四个问题——开场 12 秒没有立起钩子（核心道具到第 2 场才出现）、母题台词在场却不做
因果功、结尾是摸头、`retentionPlan` 自报的第一个钩子在 00:38（65 秒片子的过半处）——
**一条都没有提**。

**根因不是模型漏检，是判据本身的形状：这个阶段查「声称的事画面里有没有」，
当声称本身很弱时，这个检查是重言式。** S1 声称的功能是「建立生活场景与氛围，展现主角
懂事乖巧」，而画面确实做到了这件事，所以 `depicted` 是**正确判定**。问题不在没兑现，
在那个声称本身不值钱。**任何写着「建立氛围」「调节节奏」这类通用功能的场次都会永远通过。**

所以这个阶段的覆盖面比 §2.13 的措辞听起来窄：它能查 declared vs shown，
**查不出 declared 本身是不是一个值得的目标**。要查后者需要一个外部的好坏标准，
而那正是 §2.13 明确不做的事（不打总分、不设门槛，依据是 13 份综合分挤在 7.8–8.3）。
**不要靠往体检提示词里加「顺便评价一下这个功能值不值钱」来补**——那会把一个
判据清晰的确定性覆盖率检查，混成又一个没有分辨力的主观打分。

一条实现摩擦，会挡住任何不走服务端的回放：`WorkflowService` 构造函数内建的
`normalizeStageDefaults` **只覆盖九个老阶段，不含 `storyQualityReview`**
（也不含 `storyCandidateReview` / `storyCandidateRevision` /
`animationPlanReview` / `animationPlanRevision`）——那些键只在 `server.js` 的
`buildStageDefaults` 里。不显式传 `stageDefaults` 就会解析出「未知 provider」并在
调用模型**之前**就 InputError。生产路径不受影响（服务端总会传），
但这正是这个阶段长期没被真实调用过的一部分原因。

## 8. `.env` 待补两条

- `qwen3.8-max=12/36`（阿里云官方原价已核实，华北2·北京；`costCny` 目前返回 `null`，记不了账）
- `deepseek-v4-flash=输入/输出` 是模板占位符被原样粘进去了，每次启动都刷一条告警。
  补它得先查 DeepSeek 官方价格，**不得靠猜**（§5.1 码表纪律）。

`.env` 含 API Key，不在版本控制内，需要人工修改。

---

## 9. 2026-09-06 之前导出的候选无法走对照评审

**状态**：归属与状态码已修（2026-09-10），能力缺口未决。

**已修的部分**：那份 `themeVariants` 是**请求输入**，而 `createStoryCandidateReview` 直接用
`ensureOutputContract` 校验它，抛出的 `OutputContractError` 语义是「模型输出不合契约」——
原样上抛变成 HTTP 502 加一句「模型输出未通过校验」，让用户去查模型和供应商，而实际错在自己传的包。
现已按 `createAnimationPlanRevision` 的 `report` 分支同规格转成 `InputError`：400、`origin: "client"`、
逐条诊断照样带出。**校验本身没有放宽**，只是拒得诚实。

**仍然未决的部分**：那些包**依旧评审不了**。`highValueBeatMapping[].failureSignal` 与
`transformationProof` 的 `{source, replacement}` 都是 2026-09-06 之后才加的必填字段，
所以在那之前导出的候选一律过不了 strict schema。而评审恰恰是只出报告、不进 lineage、
不改任何东西的阶段——它最应该能读历史数据。

**尚未决定**：要不要像 §2.4 已下线方言那样，给缺新字段的旧候选开一条**精确白名单**降级通道。
那是**契约变更不是 bug 修复**，需要先想清楚三件事：哪些字段可缺、缺了之后
`buildStoryCandidateReviewProjection` 怎么处理（`failureSignal` 现在是**要送**给评审的），
以及降级后的报告要不要标明它评的是一份不完整的候选。

**不得**为了让旧包通过就放宽 `ensureOutputContract` 或跳过输入校验（§5 第 1 条）。

## 10. 候选阶段仍然拿不到任何原片机制陈述

**状态**：上游那一半已解决，注入这一半仍未决。

**已解决的部分**：`storyEngine` 五个子字段此前在 `briefPrompt` 里一条说明都没有、零校验器、零消费者，
于是 `turningMechanism` 的质量随包而异（三个真实包里一份是真机制、一份可用、一份写成剧情概括）。
2026-09-10 已补上五个键的定义、把 `turningMechanism` 改成 `{before, after}` 两槽位并加了确定性闸门，
结论写进 `AGENTS.md` / `CLAUDE.md` 的简报章节。真实回放确认那个反面样本变成了真正的关系理解转变。

**仍然未决的部分**：候选生成阶段**依旧收不到任何原片机制陈述**。
`variantsCreativeBriefProjection` 只送 `contentType` / `targetAudience` / `coreEmotion` /
`emotionStructure{stage,targetEmotion,intensity}` / `nonNegotiableExperience` 的前三个字段；
`storyEngine` 整个不送，`sourceDramaticValues` 只取 `dramaticValue`。

**已经排除的做法**：

- ~~把 `turningMechanism` 与七项 taxonomy 的存在性判定注入 `variantsPrompt`~~ —— **2026-09-09 实测后回退**。
  三个包 12 个候选的真实回放（约 ¥2.3）：
  - 任务型措辞 **3/12 → 6/12**（翻倍，而这正是当初要打掉的「单向帮助」框架）
  - 与原片动作稿的逐字照抄 **0/12 → 1/12**，最长连续命中 **4 字 → 15 字**
    （按 §2.4 台词闸门同一判据，连续 ≥6 字基本不是巧合）
  - 某个包的简报明写「送达任务【原片没有】」，新候选仍写出「帮村长**运送**南瓜」

  当时三条成因分不开：①有的包 `turningMechanism` 本身就是任务框架，注入它等于推模型往任务写；
  ②`variantsPrompt` 下方已有一条「`storyEngine`、`reusableHighValueBeats[].beat` 不进入正向投影、
  不能复现或补写」，改动让同一份提示词上下自相矛盾；③唯一那条照抄落在 `turningMechanism`
  写得**最具体**的那个包上——机制写得越具体越可抄。

  `test/story-candidates-prompt-mock.test.js` 里三条 `doesNotMatch` sentinel 锁住这个回退，注释带着上面的数据。

**成因 ① 现在已经消失**：简报会写出真的关系转变了（2026-09-10 的 `storyEngine` 契约）。

**② 不是「规则写错了」。** 回退之后用 sentinel 实测，那条规则点名的 8 个字段**全部确实没有下发**，
规则与现实完全一致。矛盾只在那次失败实验期间存在过，回退时一并消失。它是**再注入的前置障碍**：
任何把这些字段送进候选提示词的改动，都必须同时处理这条规则，否则同一份提示词上下又会打架。

**③「机制写得越具体越可抄」没有任何缓解**，而且新契约让简报写得**更**具体，这一侧的风险只增不减。

所以**不能因为上游修好了就直接把那三条 sentinel 翻过来重试**：要再试，得先决定怎么防照抄
（当时唯一那条 15 字照抄正落在机制写得最具体的包上），并连同那条规则一起改，然后重新做一次跨包回放。

**不得**为了让机制进去就放宽 `variantsSourceEvidenceProjection` 或删掉那条「不进入正向投影」的规则（§5 第 3、5 条）。

---

## 11. 「每拍写出最容易被写成的那个错误版本」试过并回退

**状态**：实现完整、实测充分、**因收益未证实而代价确实存在**被回退。代码在标记
`experiment/cliche-clause-2026-09-10`，要重做从那里取，不要从零写。

**做了什么**：`storyOutline[]` 加可选字段 `clicheToAvoid`，形式「不是 X，而是 Y」，
每个候选至少 2 拍带非空条款（少于 5 拍时 1 拍），同候选内两两不同，且不得只是把同拍
`dramaticFunction` 否定一遍。三条判定全是计数与字符串比较，零语义。

**依据**：十篇外部高分改写稿 61 拍里 **26 拍（43%）**带这种否定条款，而现有候选的
`dramaticFunction` 全是「建立场景，引入情境」这类通用词汇，**没有一条说这一拍不许写成什么**。

**为什么回退**（三条，按份量排序）：

1. **收益没有被证明。** 病灶是「结尾凭空多出一个与前文无关的温情道具」，
   而实测里某候选仍然让长辈在倒数第二拍掏出一件新奖励物——**而那一拍恰好写了条款，没挡住**。
2. **代价是当下的。** 在 `.env` 默认的 `qwen3.7-max` 上，三个包里**捐旧衣服只写 1 条、硬失败**，
   打枣通过，河边写生大概率通过。**约 1/3 的包会在候选生成阶段直接报错**，而这个阶段 fail-closed。
3. **它把候选阶段推向 `qwen3.8`**：3.8 上 8/8 候选达标、0 处空串，但**慢 5–7 倍**
   （140 秒 → 780–1060 秒），且 `.env` 缺它的价目，用量记账拿到 `costCny: null`（见第 8 条）。

**留下的两条实测事实，比这个字段本身有价值：**

- **模型在按数量交差。** 3.8 严格贴线：提示词说 1 就写 1/1/1/1，说 2 就写 2/2/2/2，
  八个候选无一例外；而外部高分稿是 43% 且分布不均。**闸门数得出数量，判不出「这一拍真有没有滑坡风险」。**
- **模型会给不想写的拍输出 `""` 而不是省略整个键。** 3.7 大量如此，3.8 则 20 处全部干净省略。
  这一条对**任何**未来的可选候选字段都成立：schema 若写 `nonEmptyString` 就会被空串打挂。

**已经排除的做法**：

- ~~把否定条款并进 `dramaticFunction`~~ —— 它是 `storyCandidateStructureSignature` 的输入，
  结构签名逐字拼全部拍的 `dramaticFunction` 做两两比对；加内容会让碰撞概率骤降，
  **等于悄悄削弱一个现有闸门**（§5 第 1 条）。
- ~~把闸门挂进 `ensureThemeVariantsMatchProfile` 的 `if (upstream)` 分支~~ —— **实测直接打挂 10 条既有测试**。
  `upstream` 表示「要做溯源核对」，不表示「这是一次新生成」，两件事不能共用一个开关。
  生成期独有的闸门只能放在 `createVariants` 的 `finalize` 里。
- ~~把新字段设成必填~~ —— `ensureStoryCandidateContract` 跑 strict schema，而它被
  `validateBoundCandidate`（`server.js:301`）在**用户选中候选、生成 Full Story 时**调用，
  必填会让所有已签发的旧候选在展开剧情时当场失败。
- ~~为了让 3.7 通过而把阈值降到 1~~ —— 换 3.8 实测后证明那是模型能力不是阈值定错，
  为迁就较弱模型降标准正是 §5 第 1 条禁的。

**要重做之前先想清楚**：这个字段属于「模型填一个字段就能满足」的那一族，而 2026-09-10
同一天三次同族改动（机制注入、`storyEngine` 两槽位、本条）**没有一次被证明改变了故事质量**。
详见 `docs/交接文档-2026-09-10.md` 第一节。

---

## 12. 原片场次原文被逐字抄进 `visibleAction`（Full Story 展开阶段）

**状态**：已定位并量化，**修法未定**。同期试过的一条相邻修法已排除，见下。

**现象**：`fullStory.sceneScript[].visibleAction` 里出现与 `sourceScriptReconstruction`
逐字相同的长串。按 §2.4 台词闸门的同一判据（最长连续逐字命中 ≥6 字基本不是巧合），
这是照抄而不是巧合。**它落在 `visibleAction`，而那是唯一进 Animation 阶段场次投影的可见事实字段**，
所以会一路变成镜头。

**证据**（2026-09-12，同一份导出包 `短视频创意方案-1789155939783.json` 的候选 V2
《罐装阳光与太阳味》，五次真实回放，模型与采样全同）：

| 回放 | 逐场与原片最长连续命中 |
| --- | --- |
| 基线 | **10 字**「镜头拉远，展示夕阳下」@S6 |
| A-1 | 4 字 |
| A-2 | **9 字**「奶奶露出欣慰的笑容」@S6 |
| B-1 | 4 字 |
| B-2 | 3 字 |

两处 ≥9 字的命中都在**末场**，而末场恰好是候选没写的那一场——候选 V2 只有 5 拍，
而契约要求 `sceneScript` 至少 6 场，模型必须自己造第 6 场，造出来的就从原片结尾抄。
基线那一场整体与原片 S6 同构（傍晚／夕阳／院子／长辈坐着／桌上食物／镜头拉远全景），
只把面条＋小红花换成凉茶＋西瓜。**「候选拍数 < 场数下界」是这个形状的结构诱因**，
但不能据此就去改场数下界——那是既有契约，且 5/5 次回放都产出了 6 场合法故事。

**已经排除的做法**：

- ~~给 `fullStoryPrompt` 补一条「简报里那五个原片桥段字段是原片实例、不是本片命令」~~
  —— **2026-09-12 实测后回退**。搬的是 `variantsPrompt` 那段成熟措辞，覆盖
  `reusableHighValueBeats[].beat` / `mustRetain` / `nonNegotiableExperience.samePlotDriver` /
  `sameBeatValue` / `creativeDistancePolicy`，并要求三个消费它们的输出字段
  （`experienceFidelity.plotDriver` / `.highValueBeats` / `beatSheet[].retainedValueFromBrief`）
  只写本片自己的事实。回退有两条理由，第二条更要紧：
  1. **收益无法证明**：目标指标是 `retainedValueFromBrief` 里的原片奖励机制痕迹，
     不加规则的两次是 1、1 拍，加了之后两次是 0、1 拍，基线是 2 拍——n=2 对 n=2，
     而指标跨度只有 0–2，落在既有噪声内。B-2 照样写出「外部奖励带来的惊喜感，
     将美好体验转化为情感媒介」。
  2. **打错了地方**：`retainedValueFromBrief` **全项目只有一个消费者，是页面展示**
     （`public/app.js` 渲染成「保留价值」），不进 Animation Plan；而**五次回放的场次里
     一次都没有出现奖励或转赠动作（0/5）**。也就是说原片奖励机制只污染了一个元数据声明，
     从来没有到达画面。为一个无下游消费者的字段在每次调用上永久加 601 字符提示词，
     正是 §11「模型填一个字段就能满足，而故事没变」那一族。
- ~~把那五个字段从 `fullStoryPrompt` 的投影里屏蔽掉（照 `variantsPrompt` 的做法）~~
  —— **架构上不可行，不要再试**。`experienceFidelity` 是 Full Story 的**顶层必填**对象
  （`legacy-full-story-strict.schema.json` 的 `$defs/transformationSummary`，五个子键全是
  `nonEmptyString`），其中 `plotDriver` 的来源正是 `samePlotDriver`、`highValueBeats` 的来源
  正是 `reusableHighValueBeats`；`beatSheet[].retainedValueFromBrief` 同样必填非空。
  屏蔽会让必填字段无源可写。变体阶段能屏蔽，只因为它没有 `experienceFidelity`——
  **这个不对称有结构原因，不是遗漏**。`test/full-story-prompt-narrative-scope.test.js`
  的「那五个字段仍然完整下发给展开阶段，不做屏蔽」用 sentinel 锁住这一条。

**一条登记在案但未验证的假设**（回退前就已写下，不是事后解释）：那条被回退的规则里，
自查句写的是「把这一行写的事拿回本片的 `sceneScript` 里找一遍」，而 `retainedValueFromBrief`
属于 `beatSheet`，在输出模板里排在 `sceneScript` **之前**——模型写到那一行时 `sceneScript`
还没写，自查指向了一个尚不存在的东西。同一拍的 `storyAction` 就在手边，是更贴的锚点。
**但这个假设没有被验证过**，而且即使成立也只改善那个元数据字段，不解决本条目的正题。

**下一步该查什么，尚未决定。** 真正要挡的是「原片场次原文进 `visibleAction`」，
而现有那三条相邻规则（「允许不等于必须使用」「参考片片尾字幕卡不是必须复用的构件」
「来源表达不是内容禁词」）针对的都是道具、拟声词与文字卡，**没有一条针对整句场次动作**。
可以考虑的方向都未验证，且各有代价：①给末场单独立规矩——但「候选拍数不足时第 6 场写什么」
是创作问题，写死会变成新模板；②收紧 `sourceScriptReconstruction` 的投影粒度——
但 `transformationProof` 要求「关于原片的那一半必须能在上游逐字找到依据」，砍掉就砍掉了那条闸门的依据；
③加确定性校验（对 `visibleAction` 与上游做 LCS，≥N 字即失败）——判据现成、纯算术，
但**阈值没有实证**，且 §2.12b ⑨ 已记着「LCS 反过来用是误用」的前车之鉴（那次量的是候选间相似度，
与这里量「逐字照抄」不同，但阈值同样需要真实分布支撑）。**不得凭推断先选一条。**
