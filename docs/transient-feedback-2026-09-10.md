# 临时操作提示的页面归属（2026-09-10）

用户复现：应用模型设置后关闭、重开弹窗，旧成功提示仍显示；导入测试包后返回 AI 导演、开始新 Run、进入新候选，旧导入成功提示仍显示。

修改前已在用户原标签页确认模型提示残留，未再次应用设置、导入数据或运行模型。根因是两个提示节点没有对应的结束/导航重置；导出异步回调仅检查视频 workspace epoch，不能识别同一视频下的页面或候选切换。

## 修改前验收计划

- A：`node --check public/app.js` 与 `git diff --check`；只调整临时反馈，不改模型路由、Task 状态、业务 Artifact 或包校验。
- B：真实应用函数回归模型弹窗重开、进行中改写、同页保留、离页/换候选/新 Run 清理、取消换一批、合法导入，以及迟到签发/下载回调隔离。运行 `npm test`，与修改前 18 个既有失败名称逐项比较。
- C：独立浏览器标签页加载当前前端；测试包经隔离服务正式签发/导入，不调用付费模型、不使用用户 Run。
- D：应用后重开弹窗提示为空；导入反馈在当次页面可见，离开再进入恢复帮助文字；已有剧情、模型设置保持原值。

## 验收证据

### A 静态验证

状态：通过

执行：

- `node --check public/app.js`
- `node --check test/helpers/app-ui-harness.js`
- `node --check test/transient-feedback-ui.test.js`
- `git diff --check`

实际结果：

- 三个文件语法和 diff 检查通过。单独对照 `/tmp/mimo-transient-feedback-app-before-20260910.js`，本轮生产改动仅是这两处反馈的重置与回写归属检查。

### B 自动化行为验证

状态：定向通过；全仓仍有既有失败

执行：

- `node --test test/transient-feedback-ui.test.js` 及关联 UI、恢复、预览和下载协议回归。
- `npm test`。

实际结果：

- 修改前 11 项反例/合法对照为 8 失败、3 通过；8 项失败均是旧提示残留。原始日志 `/tmp/mimo-transient-feedback-ui-mp1uxur1/before-11.log`，用修改前 app 快照在独立目录重放，没有替换生产文件。
- 新增 18 项全部通过，关联回归共 118/118。覆盖模型应用/恢复默认后重开、正在改写时保留、同页重绘保留、离页/换候选/新 Run 清理、确认与取消换一批、有效导入内部 reset，以及迟到导出成功/失败、iframe 错误和文件读取失败。当前下载错误仍显示；下载本身继续执行；旧回调不能污染新页面。
- 定向日志 `/tmp/mimo-transient-feedback-ui-mp1uxur1/final-118.log`。
- 全仓 1638 项：1614 通过、6 跳过、18 失败。与上一轮 `/tmp/mimo-full-story-controls-tests-20260910.log` 的 18 个唯一失败名称精确一致，新增失败为 0。3 项为 `exports/git-sync-2026-09-09/snapshot` 缺依赖；15 项为旧 `exports/story-editorial-lab-2026-09-08` 实验提示词锚点断言不匹配。未修改、跳过或削弱这些断言。
- 全仓日志 `/tmp/mimo-transient-feedback-tests-20260910.log`。

### C 真实运行

状态：通过

入口：

- 独立浏览器标签页加载 `http://localhost:4173` 的实际前端。
- 隔离的实际 `server.js`，`http://127.0.0.1:58241`；V1/V2 包经过正式 HTTP commit、seal、import 验签。夹具目录 `/private/tmp/mimo-transient-ui-n4_nruk8/`。

实际结果：

- 修改前真实执行“应用模型设置 → 关闭 → 重开”复现旧成功提示；加载修改后重复同一操作，重开文本为空，所有模型下拉值一致。
- 浏览器依次通过文件选择器导入 V1、V2 两份正式签发包，分别建立隔离 Run，均成功显示当次导入反馈。
- 当前前端来自修改后的静态文件，无服务端改动，不重启用户实例。收尾 `http://localhost:4173/` 返回 HTTP 200。
- 浏览器控制台错误为空；没有调用付费模型。独立测试标签页已关闭，用户原标签页未强制刷新，也未改变其输入或模型设置。
- 隔离实例记录 `final-validation-receipt.json` 确认模型生成请求为 0；58240/58241 已无监听，夹具和 server 均退出，退出码 0。

### D 用户可见结果

状态：通过

验证内容：

- 关闭并重开模型设置弹窗。
- 导入测试包，返回主题变体，再进入该候选；再次导入另一候选的新 Run 并重复导航。

实际结果：

- 当次应用后仍显示“已应用。之后所有生成请求都会使用当前模型路由。”；关闭重开后文字为空。正在进行的提示词改写保持进度和禁用状态，由自动化合法对照覆盖。
- 两次导入分别显示“已校验并隔离导入 V1/V2：完整剧情；旧媒体未混入。”；离开再进入后恢复“可导入/导出完整剧情与动画镜头生产包，避免重复请求模型。”，成功样式一并清除。
- 两份已导入剧情在离开前后渲染文本逐字相同。同页普通重绘保留当次成功提示，由实际应用函数回归覆盖。

### 修改范围

- 修改文件：`public/app.js`、`test/helpers/app-ui-harness.js`；新增 `test/transient-feedback-ui.test.js` 和本文。
- 未修改范围：模型路由值、业务 Prompt/Schema、Task 状态、Artifact、导入校验、签发与下载协议；此前代理和运行控制修改保留。
- 当前分支：`codex/story-quality-phase1`。
- 实现与自动化验收时的工作区基点：`500ec4d186e90b03a8b77f487367a74f2ece5fb4`。用户于 2026-09-10 确认本批修改验证通过；随后按功能拆分本地提交，未推送，原检查点保留为历史记录。

### 未覆盖路径

- 未重新执行付费 AI 导演生成；新 Run 重置和候选重生成的提示边界使用真实应用函数及受控网络响应覆盖。此项改动不涉及模型输出。

### 剩余风险

- 已打开的原页面仍运行加载时的前端，刷新后生效；保留原页以免丢失用户尚未保存的输入。
- 全仓仍有上述 18 项历史失败，本次不能称为全仓全绿。

### 完成结论

两处临时反馈的页面边界已修复，原始模型弹窗反例和两次实际包导入导航均通过；有效当次反馈、进行中状态及既有内容有保留证据。
