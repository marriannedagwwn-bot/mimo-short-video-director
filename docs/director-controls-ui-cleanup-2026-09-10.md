# 导演控件界面精简（2026-09-10）

按用户页面批注，删除流水线标题右侧的运行/模型/token 摘要，以及“放弃旧任务并开始新 Run”按钮；终止、暂停按钮的悬停背景从 44×44px 缩为 32×32px，点击区域保留 44×44px。

旧按钮会清空当前 Run 的全部任务、结果和媒体；终止只取消当前导演任务并保留已完成结果。此次删除旧入口和专属前端代码，保留原有终止语义、后端清理接口、更换视频时的清理，以及启动时处理其他活动任务的确认。用量仍保存在 Task 快照中，只移除该处展示。

## A 静态验证

状态：通过。

- `node --check public/app.js` 与 `git diff --check` 通过。
- `public/` 无 `pipelineUsage`、`releaseActiveTasks` 及其专属函数残留；删除 HTML 后同步移除事件、元素引用和文案写入。
- 两个按钮继续使用独立的可访问名称。共享伪元素内缩 6px，图标尺寸保持 20px。

## B 自动化行为验证

状态：相关测试通过；全仓保留原有失败。

- 导演控件与状态相关定向测试 40/40 通过。既有暂停、继续、终止、刷新恢复、阶段状态和累计用量断言保留；移除摘要文案断言，改为核对任务快照和阶段展示。
- `npm test`：1583 项，1559 通过、6 跳过、18 失败。日志：`/tmp/mimo-director-ui-cleanup-tests-20260910.log`。
- 失败名称集合与修改前 `/tmp/mimo-system-proxy-official-tests-final-20260910.log` 完全相同：3 项导出快照依赖缺失、15 项旧实验提示词锚点不匹配，无新增失败，未削弱这些断言。

## C 真实运行

状态：真实前端加载与浏览器渲染通过。

- 独立浏览器标签加载 `http://localhost:4173/` 当前前端，两个已删除 ID 的实际 DOM 数量均为 0，模型状态正常。
- 临时静态验收页使用当前 HTML 的控件片段和真实 `public/styles.css`，只展示运行态，不执行任务。两个按钮的实际点击区域均为 44×44px，伪元素背景均为 32×32px；终止按钮悬停实际呈现 `rgba(255,255,255,.16)`。
- 临时标签页和监听端口 58231 已关闭。未启动模型生成，也未重启生产服务。

## D 用户可见结果

状态：新加载页面和控件渲染已验证；用户原标签页保留当前输入。

- 流水线标题右侧不再显示运行摘要，设定面板不再显示旧 Run 清理按钮。
- 终止和暂停的背景缩小，按钮顺序和点击范围保留。
- 因原页面正在编辑字幕，没有强制刷新；刷新后加载本次前端修改。

修改范围：`public/index.html`、`public/styles.css`、`public/app.js`、两个既有 UI 测试和本记录。此前代理修改保留，未混入本次变更目的。实现与自动化验收时的工作区基点为 `500ec4d`；用户于 2026-09-10 确认本批修改验证通过，随后按功能拆分本地提交，未推送。

未覆盖路径：本次没有重跑真实付费供应商任务，也没有将早先 Run 控制功能声明为已获用户验收。
