# 浏览器工作区生命周期验收（2026-09-09）

需求：阶段数据与原视频副本、恢复地址一起保存；更换视频清空旧生产数据；关闭页面后删除应用保存的源副本和生成结果，仅长期保留“设定创作宇宙”中的七项设置。

刷新属于恢复操作。页面关闭通知或生命周期连接断开后保留 60 秒刷新宽限，服务端每 5 秒清理到期数据。后台标签页的连接仍在时不按浏览器 JavaScript 心跳超时删除；没有连接也没有关闭通知时，最后心跳起 2 分钟过期。停服期间不能执行删除，下次启动补清。用户最初选择的外部文件、主动导出文件、升级前未绑定页面的历史 Run 不进入这次自动清理范围。

## 验收证据

### A 静态验证

状态：通过

执行：

- Node.js v24.19.0；对服务端、页面客户端、存储和清理模块、媒体 worker 执行语法检查。
- `git diff --check`。

实际结果：

- 语法与空白检查通过。新增页面归属和源视频元数据位于服务端 sidecar；未向业务 Artifact 或 Task Store 增加视频字节。

### B 自动化行为验证

状态：正式测试目录通过；全目录命令仍有无关实验失败

执行：

- `node --test test/*.test.js`。
- `npm test`（项目原命令为 `node --test`，同时发现 ignored exports 内的测试）。
- 针对 workspace 存储、HTTP、客户端、清理、Debug、导入和真实媒体 worker 的定向测试。

实际结果：

- 正式测试目录 1,263 项：1,257 通过、6 跳过、0 失败。
- 最终生命周期定向组 81/81 通过。收尾时发现 HTTP 测试 teardown 尚有异步断连写入，已改为等待全部 response close、handler 和 Store 锁完成，再删除临时目录；同组重复 3 次均通过。
- 全目录 1,450 项：1,426 通过、6 跳过、18 失败。3 项来自 `exports/git-sync-2026-09-09/snapshot/test/` 缺失快照模块；15 项来自 `exports/story-editorial-lab-2026-09-08/` 的旧提示词锚点断言。未修改这些实验或缩小 `npm test` 来隐藏失败。
- 回归覆盖旧 pageId/旧 generation、源替换串行队列、导入期间换视频、坏包保留旧会话、Range/大小限制、后台连接保护、迟到关闭、连接替换、注册尚未结束即断开、过期重启、目录归属与符号链接、孤立 Run、清理时等待者、迟到媒体和 Debug。
- 媒体测试真实启动本地 HTTP 协议 fixture 与 worker，确认 Run 删除后独立 worker 不能重建视频或回执，也不向供应商请求发送本机 lifetime 路径。

### C 真实运行

状态：通过（本地产品入口与受控协议）

入口：

- 从隔离临时目录启动真实 `server.js`，地址 `http://localhost:41874/`，显式清空模型 API key，使用演示流程。
- 本机开发服务 `http://localhost:4173/`。

实际结果：

- 使用 FFmpeg 生成两份可播放输入：A 为 4 秒测试图，B 为 5 秒蓝色视频。
- 上传 A 并完成五阶段流程后，Run metadata 中的源地址为 `/api/browser-workspace/<UUID>/source`，SHA-256 与保存的二进制一致；原视频可播放，重新抽取 6 帧。
- 同一标签页刷新、隔离服务停止后重新启动再刷新，均恢复 A 和五阶段已完成结果，没有重新调用 provider。
- 换成 B 后，旧 A 副本、旧 Run 和该 Run 下的图片/视频验收文件全部 `ENOENT`；B 能重新完成流程。最初选择的 A/B 文件仍在。
- 最终版本真实关闭标签页时，服务端记录了 60 秒关闭截止；随后复核 workspace、源副本、Run、图片和视频目录全部不存在，旧源 URL 返回 HTTP 404。
- 另在隔离状态目录创建一个带页面归属但未 attach 的 Run 和一个无归属对照 Run；重启真实服务后，前者已删除，后者仍存在，workspace 目录为空。
- 本机 4173 首页 HTTP 200，`/api/health` HTTP 200、`ok: true`、`mode: live`。这仅证明本机服务可用，不代表执行了收费模型生成。
- 本机 4173 返回的 workspace client 与当前磁盘文件逐字一致；运行进程为 Node 24.19.0，启动时间晚于最终服务端改动。

### D 用户可见结果

状态：通过

验证内容：

- 浏览器实际上传、运行、刷新、替换文件、关闭标签页、重新打开页面。

实际结果：

- 刷新后原视频 `readyState=4`、时长 4 秒，五阶段均显示“完成”。
- 更换视频时旧结果立即清空，五阶段恢复“等待”，新视频随后可运行。
- 关闭后重新打开，视频、字幕、结果均为空；固定角色、赛道、创作限制、表情规则、候选数量、画幅、时长七项设置保留。
- 验收发现内置浏览器关闭标签页不发送 pagehide，因此增加了实际连接断开检测；未仅凭页面关闭事件的代码存在宣称通过。

### 修改范围

- 修改文件：页面 `public/app.js` 和新增 workspace client；`server.js`；新增 workspace store、HTTP、cleanup、debug 和媒体 lifetime 模块；Durable Task 等待/上下文、Production State 导入、镜头视频生成器和 worker；相应测试、运行目录忽略规则及生命周期文档。
- 未修改范围：现有剧情与角色业务契约。脏工作区里并行存在的原片来源、提示词、文本用量等修改原样保留，本轮未提交或推送 Git。
- 当前分支：`codex/story-quality-phase1`。
- 当前 HEAD：`0fd4f44ff369720d0db3617435a761532b7a8542`。

### 未覆盖路径

- 未执行收费文本、图片或视频供应商端到端生成；供应商端已提交的调用无法由页面清理撤回。
- 未实测每一种浏览器崩溃、系统强制关机或网络断开方式；连接与时钟边界有自动化覆盖。
- 未将历史无页面归属的数据迁入清理范围；历史导出包仍不包含原视频字节。

### 剩余风险

- 页面持续断连超过宽限会按关闭处理；停服超过有效期，原标签页在下次启动后也不能恢复到期工作数据。需要长期保留的成果须主动导出。
- `npm test` 的 18 项实验目录失败仍在，应由对应实验任务处理。

### 完成结论

新浏览器工作区的源视频保存与恢复、换片清理、关页物理删除、七项设置保留已通过真实本地服务和浏览器验收。业务生成质量与远端取消能力不在本次证明范围。

原始本机验收日志及删除记录保存在 `/tmp/mimo-browser-workspace-acceptance.zF7n9Q/`，该临时目录可能被系统清理；本文件记录已观察结果。
