# 交接文档（2026-09-10）

给下一个上下文窗口。分支 `codex/story-quality-phase1`，工作区干净，今天 4 个提交全部落地。

---

## 一、今天最重要的一条结论

**凡是「模型填一个字段就能满足」的约束，它都能满足，而故事没变。今天三次全中同一个形状：**

| 改动 | 模型的反应 | 质量结果 |
| --- | --- | --- |
| 把原片机制注入候选提示词 | 照做了 | ❌ 任务型措辞 **3/12 → 6/12**，逐字照抄原片 **0/12 → 1/12**（最长 15 字）→ **已回退** |
| `storyEngine.turningMechanism` 改两槽位 | 照做了 | ⚠️ 反面样本修好，**旧值最好的那个反而退步**（笼统化 + 一句与原片不符的断言） |
| `storyOutline[].clicheToAvoid` | 照做了，8/8 精确写 2 条 | ❌ 病灶（结尾凭空多出温情道具）**在写了条款的那一拍照样发生** |

三个包的 `turningMechanism` **全部**以「观众以为…／观众重新理解为…」开头，也是同一件事：模型在照抄措辞框架。

**今天唯一真正起作用的改动形状不同**：评审的机制收敛不是「让模型多写点什么」，而是**把清单封到 4 条上限**——「4 个候选写出 8 条互不相同的原片机制」从此在构造上不可能。同类的还有时长窗口（纯算术，候选合计在窗口内 0/12 → 11/12）与拍号派生。

> **区别是：改变可能性，而不是增加要求。**

`docs/高分改写稿结构分析-2026-09-09.md` 里还剩 ③（`change` 以删除开头）与 ④（可证伪的 `preserve`）没做。**它们和上面失败的三次是同一族**（都是新加字段让模型填），直接做大概率是第四次重复。

---

## 二、待办：一个已经发出去、还没回来的实验

用户手上有一份**盲评材料**，这是当前最有价值的未决数据。

- 材料：scratchpad 的 `候选盲评-2026-09-10.md`，**打枣组 16 个 + 捐旧衣服组 12 个**，其中 20 个是今天新生成的、8 个是包里的旧候选作对照，**来源已打乱且不标注**
- 答案对照：同目录 `blind-key.tsv`（代号 → 来源/模型/原 ID）
- 要用户做的：每组挑 1–2 个「会真的拿去做成片的」，各写一句为什么

**它的价值在于可证伪**：今天三次改动都只能证明「模型照做了」，证明不了质量。而用户是这套系统真正服务的评判者——`AGENTS.md` 已记着他两次独立选片都选了结尾「生活多了一点东西」的候选，那是真人偏好，比任何模型评分都硬。

拿到结果后：
- 新候选没有明显胜过旧候选 → **停止逐字段加约束**，把结论写进 `docs/待解决项.md`
- 有明显差异 → 才有依据继续做 ③④

---

## 三、模型变量（用户提出，已实测）

**今天绝大部分回放跑在 `qwen3.7-max` 上**（`.env` 所有阶段默认值），而 debug 侧车显示 `qwen3.8-max` 的 7 次调用**全部**落在 `animationPlanReview`(5) 与 `animationPlanRevision`(2)——即用户凭印象说的那两个阶段。

实测对照（同样三个包、同样提示词）：

| | 3.7-max | 3.8-max-0902 |
| --- | --- | --- |
| `clicheToAvoid` 写够 2 条 | 1/2 包失败 | **8/8 候选达标** |
| 该省略的键写空串 | 大量 | **0 处，全部干净省略** |
| `narrativeMode` 分布（要 2 个生活型） | ✅ 满足 | ❌ **两次都只给 1 个** |
| 耗时 | 138–347 秒 | **782–1060 秒（慢 5–7 倍）** |
| completion token | 13.8k–14.5k | 28k–40k（§2.7 实测 82% 是推理） |
| 用量记账 | 正常 | **`costCny: null`**（`.env` 缺价目，待解决项 #8） |

**`qwen3.8-max-0902` 与不带日期的 `qwen3.8-max` 是同一个模型**（耗时与 token 同量级）。

**结论：3.8 不是全面更好。** 它在一条计数指令上稳定优于 3.7，在另一条上稳定劣于 3.7。选模型不能只看一条。

---

## 四、今天的 4 个提交

```
6b9737f feat: 候选每拍补 clicheToAvoid（阈值 2，只在 createVariants 生成路径校验）
fe6f5e7 fix: 旧候选走评审时报 InputError 而不是「模型输出未通过校验」的 502
925a71b feat: storyEngine 五个子字段写定义，turningMechanism 拆成 {before, after}
c4fb9f1 feat: 评审的原片机制清单改为全批共享、候选按 id 引用
```
（更早还有 `871a97b` 时长目标三处缺口补齐、`ce2385d` CLAUDE.md 与上游三处变更同步。）

**两处架构约束，改候选契约前必须先看**：

1. **不能往 `dramaticFunction` 里加内容**——它是 `storyCandidateStructureSignature` 的输入，结构签名逐字拼全部拍的 `dramaticFunction` 做两两比对；加内容会让碰撞概率骤降，**等于悄悄削弱一个现有闸门**。
2. **候选的新必填字段会打挂旧候选**——`ensureStoryCandidateContract` 与 `ensureThemeVariantsMatchProfile` 都被 `validateBoundCandidate`（`server.js:301`）调用，那是**用户选中候选、生成 Full Story 时**跑的。生成期独有的闸门要放在 `createVariants` 的 `finalize` 里。
   曾试过挂在 `ensureThemeVariantsMatchProfile` 的 `if (upstream)` 分支上，**直接打挂 10 条既有测试**——`upstream` 表示「要做溯源核对」，不表示「这是一次新生成」。

---

## 五、环境与操作陷阱（今天真的踩过，都花了时间）

- **`TaskStop` 只杀包装进程，node 会活下来。** 4174 上会留下一个**跑着旧代码**的服务，而健康检查照常通过——差点用旧代码做回放。用 `netstat -ano | grep :4174` 找 PID 再 `taskkill //PID {} //F`。
- **后台命令不要接 `| head -N`。** 管道关闭会杀掉进程，今天因此白跑掉一个包。
- **导出包不能直接回放 `/api/brief`。** `groundedStageInput` 对 `sourceScriptReconstruction` **无条件验签且没有出口**（`WORKFLOW_SIGNATURE_POLICY` 只覆盖角色边界签名），外来包一律 400。要真实回放简报只能**从源视频重跑 analyze → reconstruct → brief**。
- **`/api/variants` 可以直接 POST**，它不验 grounding seal。
- **改了服务端文件必须重启服务**（无 watch）。
- 写脚本时：`node -e '...'` 配 heredoc 会**多吃一层反斜杠**，含 `\r\n`、`\*` 的正则一律写进 `.mjs` 脚本文件再执行。

---

## 六、数据在哪

- **六份真实导出包 + 六支参考片 MP4 + 字幕 RTF**：`C:/Users/QinFeng/Downloads/ChatGPT交付文件/`
  - ChatGPT 只评过 4 支参考片（8.6–9.2 分）与 20 个候选（4.5–7.4 分）
  - **《累了就歇会》《这一站，先别难过》两支没评过也没用过**，可作扩样本
  - `打枣.mp4` 的 SHA-256 与包内 `sourceVideo.digest` 逐字节一致，确认是同一文件
- **今天所有回放结果**：scratchpad 的 `cl-*.json`（3.7）、`r38-*.json`（3.8）、`r0902-*.json`（3.8-0902）、`chain-*.json`（简报全链）
- **分析文档**：`docs/高分改写稿结构分析-2026-09-09.md`
- **未决问题**：`docs/待解决项.md`，今天动过 #9（归属已修、能力缺口未决）与 #10（上游已解决、注入仍未决）

---

## 七、明确不要做的

- **不要因为「简报侧修好了」就把那三条 `doesNotMatch` sentinel 翻过来重试机制注入**（`test/story-candidates-prompt-mock.test.js`）。当时三条成因里，②「提示词自相矛盾」与 ③「机制写得越具体越可抄」**原样还在**，而新契约让简报写得**更**具体，③ 的风险只增不减。
- **不要为了让模型通过而降低已定的阈值**。今天已经栽过一次：`clicheToAvoid` 阈值曾从 2 降到 1 去迁就 3.7，换 3.8 实测后改回——§5 第 1 条。
- **不要只跑一个包就下结论**。今天栽过两次：「召回率很低」从单包得出、几小时后跨包统计是 38%；「照抄是普遍现象」也是单包，跨包是 1/12。
